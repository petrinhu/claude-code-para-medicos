# Design — Aula 16: Scaffold do ClinMd-Tribe

**Data:** 2026-06-08  
**Duração prevista:** ~43 min  
**Estrutura:** 9 seções lineares  
**Regra inviolável:** aluno NUNCA abre `.py` para editar — só prompta e valida  
**Formato:** Gravado em take único no OBS. Unidirecional — sem interação com alunos.

---

## Objetivo de aprendizagem

Ao final da aula, o aluno consegue:

1. Gerar a estrutura completa de pastas e arquivos-base do ClinMd-Tribe via um único prompt ao Claude Code
2. Identificar cada camada na estrutura gerada usando a analogia do plantão
3. Explicar o papel do `__init__.py` com a analogia da credencial do andar
4. Entender que só uma pasta muda quando um componente específico muda (independência entre camadas)

---

## Gancho de abertura

Contraste visual: `type arquitetura.txt` mostra o mapa das 4 camadas. Em seguida, `dir /s /b` mostra que as pastas não existem ainda no projeto. "A planta está no papel. O prédio, não. Isso muda agora."

---

## O que o scaffold gera

Um único prompt ao Claude Code produz:

- 4 pastas: `presentation/`, `application/`, `domain/`, `infrastructure/`
- `__init__.py` em cada pasta com comentário clínico de 1 linha:
  - `presentation/` → `# Recepção — telas e interação com o usuário`
  - `application/` → `# Triagem — orquestração do fluxo clínico`
  - `domain/` → `# Médico — regras clínicas e protocolos`
  - `infrastructure/` → `# Laboratório — arquivos, banco e persistência`
- 1 placeholder por camada com docstring clínica + função `camada()`:
  - `presentation/tela_inicial.py`
  - `application/orquestrador.py`
  - `domain/calculadoras.py`
  - `infrastructure/armazenamento.py`
- `main.py` atualizado que importa `camada()` de cada módulo e imprime 4 linhas antes de abrir o Flet

### Prova de integração — 4 linhas no terminal

```
Recepção pronta
Triagem pronta
Médico pronto
Laboratório pronto
[janela Flet abre]
```

Sem traceback = estrutura válida. Critério de "feito" verificável a olho nu.

---

## Prompt canônico da aula

```
No projeto ClinMd-Tribe, cria o scaffold completo da Clean Architecture.

Faça exatamente isto:

1. Cria as pastas: presentation, application, domain, infrastructure

2. Em cada pasta, cria um __init__.py com um comentário de uma linha
   explicando o papel clínico da camada:
   - presentation:   # Recepção — telas e interação com o usuário
   - application:    # Triagem — orquestração do fluxo clínico
   - domain:         # Médico — regras clínicas e protocolos
   - infrastructure: # Laboratório — arquivos, banco e persistência

3. Cria um arquivo placeholder em cada pasta (sem lógica funcional ainda):
   - presentation/tela_inicial.py
   - application/orquestrador.py
   - domain/calculadoras.py
   - infrastructure/armazenamento.py

   Cada placeholder deve ter:
   - Docstring clínica explicando o que vai morar nessa camada
   - Uma função chamada camada() que retorna o nome da camada em português

4. Atualiza o main.py para importar camada() de cada módulo e imprimir
   no terminal, antes de abrir a janela Flet:
   Recepção pronta
   Triagem pronta
   Médico pronto
   Laboratório pronto

5. Garante que uv run python main.py roda sem erro de import.
   Me explica em uma linha o que fez em cada camada.
```

---

## Analogia do `__init__.py`

Explicado com 1 parágrafo de analogia clínica: credencial do andar. Em Python, uma pasta com `__init__.py` é reconhecida como módulo — como um departamento hospitalar com identidade própria. Sem ele, o `main.py` não consegue importar a camada. É como um andar sem placa no painel do elevador: o andar existe, mas ninguém sabe como chegar lá.

---

## Tour pelo código gerado (Seção 3)

Professor abre e narra cada arquivo. Foco prioritário em `domain/calculadoras.py` — o médico reconhece o território clínico na docstring. Ênfase: este arquivo não sabe se há interface gráfica, não sabe se há banco de dados. Só sabe medicina.

---

## Transferência retórica — 3 perguntas (Seção 4)

| Pergunta | Resposta | Camada |
|----------|----------|--------|
| Trocar Flet por uma API web | só `presentation/` muda | Apresentação |
| Diretriz CHA2DS2-VASc muda (idade > 75 = 3 pts) | só `domain/` muda | Domínio |
| SQLite → PostgreSQL | só `infrastructure/` muda | Infraestrutura |

Professor pergunta, professor responde. Formato retórico — a aula é gravada.

---

## Consultas aos agentes (Seções 5–7)

### @software-architect
```
@software-architect, o ClinMd-Tribe está usando Clean Architecture
com 4 camadas: presentation, application, domain, infrastructure.
Cada camada tem __init__.py e um placeholder.
As dependências apontam para dentro — domain não importa nada das outras camadas.
Essa estrutura é robusta para crescer com calculadoras clínicas,
anotador e RAG local? Algum risco arquitetural a antecipar?
```

### @caetano-cto
```
@caetano-cto, o scaffold do ClinMd-Tribe está gerado e rodando.
4 pastas, 4 __init__.py, 4 placeholders, main.py integrado.
uv run python main.py imprime as 4 camadas e abre a janela Flet sem erro.
Validação técnica: estamos prontos para começar as calculadoras clínicas?
```

### @capitolino-cpo
```
@capitolino-cpo, o scaffold do ClinMd-Tribe está pronto.
Estrutura em Clean Architecture — presentation, application, domain, infrastructure.
O próximo módulo implementa as calculadoras clínicas (CHA2DS2-VASc, PHQ-9, GAD-7).
Essa arquitetura suporta o roadmap completo do produto?
```

---

## Estrutura das seções

| # | Título | Duração |
|---|--------|---------|
| 1 | Abertura — A Planta e o Prédio | 3 min |
| 2 | Prompt de Scaffold | 12 min |
| 3 | Tour pelo Código Gerado | 10 min |
| 4 | Transferência Retórica — 3 Perguntas | 4 min |
| 5 | @software-architect | 3 min |
| 6 | @caetano-cto | 3 min |
| 7 | @capitolino-cpo | 3 min |
| 8 | /tab_pendencias | 2 min |
| 9 | Encerramento + Dever de Casa | 3 min |
| **Total** | | **~43 min** |

---

## Dever de casa

O aluno anota (papel ou arquivo de texto, não no código):

1. Qual calculadora quer implementar primeiro (CHA2DS2-VASc, PHQ-9, GAD-7, ou outra da prática clínica)
2. Em qual camada da Clean Architecture o cálculo vai morar
3. Qual seria o primeiro prompt para o Claude Code começar

---

## Riscos técnicos a antecipar no roteiro

| Risco | Causa | Mitigação |
|-------|-------|-----------|
| `ModuleNotFoundError` | `__init__.py` faltando | Prompt já força criação explícita |
| Import quebra | Rodar de subpasta em vez da raiz | Lembrete: sempre rodar de dentro de `clinmd-tribe/` |
| Claude cria lógica a mais | Instinto de "ajudar" | `(sem lógica funcional ainda)` no prompt |
| Janela não abre | main.py corrompido pelo Claude | Plano B: pedir ao Claude para mostrar o main.py e corrigir |
